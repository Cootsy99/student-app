<p style="color: green"><%= notice %></p>

<%=link_to "Add Student", new_student_path %>
<%=link_to "Add Instructor", new_instructor_path %>



<h1>Courses</h1>

<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Show this course", course %>
    </p>
  <% end %>
</div>

<%= link_to "New course", new_course_path %>
